<?php
    
    function cleanData() {
        
        // Checks that all fields have been set and if not, returns false.
        // Checks input values for illegal characters and removes them. 
        // Verifies if all data is correctly formatted and cased (emails, salaries, names, etc).
        // Appends/prepends quote marks if needed.
        
        // ASSIGNS ALL CLEANSED DATA TO A NEW VARIABLE.
        
    }

?>